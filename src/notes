- add views to exam
- dataype.enum => allowReportStatus, viewType

PACIENTE:

1) POST: /api/login
<- Token

2) Criar middleware para validar authorization

3) POST: /api/exams/search
Authorization: {token}

-> Retornar lista de exames do paciente logado;
-> Passar no body filtros para aplicar na listagem;

*************************
